# Runbooks

Operational checklists and incident response notes live here.

## Release Notes

- Password reset tokens are now stored as SHA-256 hashes. Any reset links generated before this change are invalid; users should request a new reset link.
